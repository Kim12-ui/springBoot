<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
	<head>
		<meta charset="UTF-8">
		<title>방명록</title>
		<link rel="stylesheet" th:href="@{/css/forGuestList.css}">
		<script th:src="@{/js/forGuestList.js}"></script>
	</head>
	<body>
		<div class="top">
	        <h2>방명록</h2>
	        <a th:href="@{write}">
		        <img th:src="@{/images/icon_write.png}">
		    </a> 
		    <a th:href="http://http://localhost:9994/web4/" id="home">
		    	<img th:src="@{/images/icon_home.png}">
		    </a>   	
    	</div>
		<div class="guest-list-container">
			<div class="guest-entry" th:each="guest, iterStat:${guestList}">
				<div>
					<p><span th:text="${guest.num}"></span>번글</p>
				</div>
				<div>
					<p>작성자 이름: <span th:text="${guest.name}"></span></p>
				</div>
				<div>
					<p>작성시간: <span th:text="${#temporals.format(guest.inputdate, 'yyyy-MM-dd HH:mm:ss')}"></span></p>
				</div>
				<div class="message">
					<p th:text="${guest.message}"></p>
				</div>
					<input type="password" th:id="${guest.num}" placeholder="비밀번호를 입력하세요" >
				<div class="forButton">
					<input type="button" value="삭제"
							th:tnum="${guest.num}"
							th:onclick="deleteFunc(this.getAttribute('tnum'))">
				</div>
			</div>
		</div>
	</body>
</html>